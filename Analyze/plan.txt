1. 首先，为了保证后续处理的统一与便捷，先将所有内容处理为统一的标准格式(统一格式，统一参数名称)
运行有的参数使用手动输入()
    输入格式：
        A100：  key-value(value is json format) GPU利用率(%)、显存利用率(%)、总显存(Bytes)、利用的显存(Bytes)、空闲的显存(Bytes)、功率(mW)、温度(C)
        4090：  key-value(value is json format) GPU利用率(%)、显存利用率(%)、总显存(Bytes)、利用的显存(Bytes)、空闲的显存(Bytes)、功率(mW)、温度(C)
        NX:     key-value(value is json format) GPU利用率(%)、总显存(Bytes)、利用的显存(Bytes)、空闲的显存(Bytes)、功率(mW)、温度(C)
        天数100：key-value(value is json format) GPU利用率(%)、显存利用率(%)、总显存(MiB)、利用的显存(MiB)、空闲的显存(MiB)、功率(W)、温度(C)
        天数150：key-value(value is json format) GPU利用率(%)、显存利用率(%)、总显存(MiB)、利用的显存(MiB)、空闲的显存(MiB)、功率(W)、温度(C)
        寒武纪： csv format                      GPU利用率(%), 利用的显存(MiB), 功率(W), 温度(C), 总显存(手动提供:42396MiB)
    标准格式：
        统一使用CSV格式
    统一参数名称(内部名称)：
        train: (合理利用try语句，并及时抛出报错) Baseline{与后面的数据一致}、GPU利用率、显存利用率、总显存、利用的显存、空闲的显存、功率、温度
        test:  (合理利用try语句，并及时抛出报错) Baseline{与后面的数据一致}、GPU利用率、显存利用率、总显存、利用的显存、空闲的显存、功率、温度
    [ps:允许有的参数手动输入：Batch num,Load time,Average Forward delay,Total Inference time]
    保存到统一格式：(中间文件路径里面带一个json描述文件，后续可以使用统一的代码像load模型一下直接对该路径下内容进行处理)

2. 进行计算并输出最终结果
    结果要求：
    ——训练：
        训练时间：日志文件中已有
        训练能耗：使用日志文件中的功耗随时间关系进行乘积和计算
        吞吐量：  总样本数(Items) / 训练时间
        能效比：  总样本数(Items) / 训练能耗
        硬件利用率：使用日志文件中的GPU利用率随时间关系计算平均值
        单卡显存占用： 使用日志文件中的显存占用随时间关系计算平均值
    ——推理：
        平均前向推理速度：总样本数(Items) / 推理时间
        数据加载时间：    日志文件中已有
        平均前向推理时延：日志文件中已有 
        能耗：           使用日志文件中的功耗随时间关系进行乘积和计算
        能效比：          总样本数(Items) / 训练能耗
        硬件利用率：      使用日志文件中的GPU利用率随时间关系计算平均值
        单卡显存占用：    使用日志文件中的显存占用随时间关系计算平均值